/*

import { writable } from 'svelte/store';
import type { Player, BoardState, Deck } from '../Models/types';

const initialState = {
  players: [], // Array von Player-Objekten
  boardState: {}, // Zustand des Spielbretts
  infectionDeck: {}, // Zustand des Infektionsdeck
  playerDeck: {}, // Zustand des Spielerdeck
  gameIndicators: { // Spielstatusindikatoren
    infectionRate: 0,
    outbreaks: 0,
    // Weitere Indikatoren
  },
  // Weitere Spielvariablen
};

const gameState = writable(initialState);

export default gameState;

*/